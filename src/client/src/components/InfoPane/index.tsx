import * as React from "react";
import styles from "./styles.module.css";

interface IProps {
  title: string;
  isVisible: boolean;
  disabled?: boolean;
}

const InfoPane = (props: IProps) => {
  return (
    <div>

    </div>
  );
};

export default InfoPane;